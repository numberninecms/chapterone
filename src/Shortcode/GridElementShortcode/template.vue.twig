{% verbatim %}
<div :class="[
        'col-span-' + parameters.span,
        'relative',
        {
            'p-3': children && children.length > 0,
            'p-5': !(children && children.length > 0),
        }
    ]">
    <PageBuilderComponent v-for="component in children" :key="component.id" :component="component" />
</div>
{% endverbatim %}
